language: node_js

node_js:
  - "8"

install:
  - yarn

script:
  - yarn test:report

before_deploy:
  - npm i -g now

deploy:
  provider: script
  script: now -t $NOW_TOKEN -A now.demo.json && now -t $NOW_TOKEN -A now.eds.json && now -t $NOW_TOKEN -A now.opendk.json
  on:
    branch: master
