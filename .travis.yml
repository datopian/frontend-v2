language: node_js

node_js:
  - "8"

install:
  - yarn

script:
  - yarn test:report

before_deploy:
  - npm i -g now
  - now switch datopian1

deploy:
  provider: script
  script: now -t $NOW_TOKEN -A now.demo.json && now -t $NOW_TOKEN -A now.eds.json
  on:
    branch: master
